<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:drawable="@drawable/background_white" android:state_checked="true" />
    <!-- Default state -->
    <item android:drawable="@android:color/transparent" />
</selector>